name: triple_h
description: "capstone project"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ">=3.5.4 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

#  qr_code_scanner: 1.0.1
  flutter_background: ^1.3.0+1
  flutter_gemini: ^2.0.3
  dash_chat_2: ^0.0.20
  slide_to_act: ^2.0.2
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  #------------- Environment -------------#
  flutter_dotenv: 5.2.1
  #------------- icons -------------#
  cupertino_icons: ^1.0.2
  iconsax: ^0.0.8

  #------------- state -------------#
  get: ^4.6.6
  get_storage: ^2.1.1

  #------------- Utility Packages -------------#
  http: ^1.1.0
  #intl: 0.19.0
  intl: ^0.18.0
  logger: ^2.0.2+1
  lottie: 3.1.3
  shimmer: ^3.0.0
  image_picker: ^1.0.5
  cached_network_image: ^3.3.0
  connectivity_plus: ^5.0.2
  url_launcher: ^6.1.14
  flutter_native_splash: ^2.3.3
  smooth_page_indicator: ^1.1.0
  google_fonts: ^6.1.0
  pagination_flutter: ^0.0.8

  #------------- Product Specific -------------#
  readmore: 3.0.0
  carousel_slider: 5.0.0
  flutter_rating_bar: ^4.0.1
  capped_progress_indicator: ^0.0.2
  #-------------chart------------------------#
  fl_chart: 0.69.0
  #------------- Weather widget -------------#
  #------------- Firebase -------------#
  firebase_core: 3.6.0
  firebase_auth: 5.3.1
  cloud_firestore:  5.4.4
  google_sign_in: 6.2.2
  firebase_storage: 12.3.4
  firebase_database: 11.1.4
  #---------------Weather Widget------------------#
  weather_icons: 3.0.0
  weather_animation: ^1.1.0
  weather: ^3.1.1
  #-----------------Location widget---------------#
  geolocator: ^10.0.0  # use the latest version from pub.dev
  geocoding: ^2.0.0
  #-----------------Multi Selection ---------------#
  multi_select_flutter: ^4.1.3
  #------------- switch -------------#
  lite_rolling_switch: ^1.0.1
  #------------SVG Flutter ----------#
  flutter_svg: ^2.0.0
#------------Chart ----------------#
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  build_web_compilers: ^4.0.4
  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: 5.0.0
  test: ^1.25.8

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  #--------------- LOCAL ASSETS ------------------#
  assets:
    - assets/icons/dashboardIcon/
    - assets/images/onboarding_images/onboarding2/
    - assets/logos/search_bar_logo/
    - assets/images/onboarding_images/onboarding3/
    - assets/images/onboarding_images/
    - assets/images/onboarding_images/onboarding1/
    - assets/images/profile_image/
    - assets/logos/green_farm_logo/
    - assets/images/background_login/
    - assets/images/animations/
    - assets/images/plants/tomato/
    - assets/logos/
    - assets/icons/dashboardIcon/analytics/dark_mode/
    - assets/icons/dashboardIcon/analytics/white_mode/
    - assets/icons/dashboardIcon/irrigation/dark_mode/
    - assets/icons/dashboardIcon/irrigation/white_mode/
    - assets/icons/dashboardIcon/my_farm/dark_mode/
    - assets/icons/dashboardIcon/my_farm/white_mode/
    - assets/icons/dashboardIcon/profile/dark_mode/
    - assets/icons/dashboardIcon/profile/white_mode/
    - assets/icons/adding_button/
    - assets/icons/sensorIcon/humid/
    - assets/icons/sensorIcon/soil/
    - assets/icons/sensorIcon/temp/
    - assets/icons/weatherIcon/inday/clouds/
    - assets/icons/weatherIcon/inday/rain/
    - assets/icons/weatherIcon/inday/sun/
    - assets/icons/weatherIcon/inday/thunderstorm/
    - assets/icons/weatherIcon/night/cloudNight/
    - assets/icons/weatherIcon/night/midnightClear/
    - assets/icons/weatherIcon/night/nightRain/
    - assets/icons/weatherIcon/night/nightRainThunder/
    - assets/icons/animationLottie/
    - assets/icons/iconAdding/
    - assets/icons/addingTreeIcon/
    - assets/icons/iconAdding/
    - assets/images/plants/foodCrop/
    - assets/images/plants/fruit/
    - assets/images/plants/herb/
    - assets/images/plants/industrialCrop/
    - assets/images/plants/vegetable/
    - assets/images/plants/tomato/
    - assets/images/waterLevel/25Percent/
    - assets/images/waterLevel/50Percent/
    - assets/images/waterLevel/75Percent/
    - assets/images/waterLevel/100Percent/
    - assets/images/outputDevices/pump/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: Poppins
      fonts:
        - asset: assets/fonts/Poppins-Light.ttf
          weight: 300
        - asset: assets/fonts/Poppins-Regular.ttf
          weight: 400
        - asset: assets/fonts/Poppins-Italic.ttf
          style: italic
        - asset: assets/fonts/Poppins-Medium.ttf
          weight: 500
        - asset: assets/fonts/Poppins-MediumItalic.ttf
          weight: 500
          style: italic
        - asset: assets/fonts/Poppins-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Poppins-Bold.ttf
          weight: 800
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
